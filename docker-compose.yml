services:
  aabot:
    image: ${IMAGE_NAME}
    env_file:
      - .env
    volumes:
      - type: volume
        source: mementodb
        target: /usr/src/app/data
    logging:
      driver: "local"
      options:
        max-file: "2"
        max-size: "10m"
    
volumes:
  mementodb:
    name: mementodb
