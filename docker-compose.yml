services:
  aabot:
    image: ${IMAGE_NAME}
    env_file:
      - .env
    volumes:
      - type: volume
        source: mementodb
        target: /usr/src/app/data
    logging:
      driver: "json-file"
      options:
        max-size: 50m
    
volumes:
  mementodb:
    name: mementodb
