services:
  aabot:
    image: ${IMAGE_NAME}
    env_file:
      - .env
    volumes:
      - type: volume
        source: mementodb
        target: /usr/src/app/data
    logging:
      options:
        max-file: "1"
        max-size: "50m"
    
volumes:
  mementodb:
    name: mementodb
